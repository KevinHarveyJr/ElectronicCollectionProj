@model IEnumerable<Electronic_Collection.Models.Item>

@{
    ViewData["Title"] = "GamesIndex";
}

<h1>GamesIndex</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReleaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GenreObj)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypeObj)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReleaseDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GenreObj.GenreId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TypeObj.TypeId)
                </td>
                <td>
                    @*<a asp-action="Edit" asp-route-id="@item.ItemId">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.ItemId">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.ItemId">Delete</a>*@
                </td>
            </tr>
        }
    </tbody>
</table>


<a asp-action="SearchByTitle">Search For Games By Title</a> |

<a asp-action="SearchByReleaseDate"> Search For Games By Release Date</a>

<a asp-action="SearchByGenre"> Search For Games By Genre</a>

<a asp-action="SearchByTags"> Search For Games By Tags</a>

<a asp-action="PlatformIndex"> Search For Platform</a>